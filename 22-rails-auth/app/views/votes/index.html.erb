<h1>
  <%= "#{@current_user.username}'s Votes" %>
</h1>

<ul>
  <% @current_user.votes.each do |vote| %>
    <li>
      Voted for: <%= vote.color.hex %>
      <%= button_to "X", "/votes/#{vote.id}", method: :delete %>
    </li>
  <% end %>
</ul>
